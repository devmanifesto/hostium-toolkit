# Hostium Toolkit WHM Plugin (addon style)

Este plugin agrega una entrada visible en WHM > Plugins usando la convención 'addon_'.

### Instalación

```bash
chmod +x install.sh
sudo ./install.sh
```

Luego, accedé a WHM > Plugins > Hostium Toolkit
